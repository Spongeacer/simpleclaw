/**
 * SimpleClaw Core — Agent Pool interface
 * Manages sub-agent lifecycle for multi-agent collaboration.
 */

// IAgentPool, SpawnOptions, SpawnResult are defined in interfaces.ts (contract layer)
export type { IAgentPool, SpawnOptions, SpawnResult, AgentRole } from "./interfaces.js";
